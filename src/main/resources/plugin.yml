name: ${artifactId}
version: ${version}
main: ${main.class}
api-version: 1.16
permissions:
  c4.admin:
    description: Allows administration of Connect4.
    default: op
commands:
  c4:
    description: Connect4 administration commands.
    permission: c4.admin
    usage: /c4
    aliases: connect4
